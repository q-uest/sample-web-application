pipeline {
    agent any
    environment {
        BUILD_COMPLETE=false
        STATUS_CODE=0
    }
    stages {
        
        stage('Build') {
            failFast true
            parallel {
                stage('Building') {
                when { environment name: 'BUILD_COMPLETE', value: 'false' }
                    steps {
                        
                        sh "mvn clean install  |  tee output.log"

                        sh '! grep "WARNING" output.log'

                        script {
                            BUILD_COMPLETE = true
                        }
                    }
                }
                stage('Monitoring the logs!!') {
                        steps {
                        script {
                            
                            while ( STATUS_CODE != 999 ) 
                            {
                                
                                STATUS_CODE = sh 'grep -q "WARNING" output.log; if [ $? -eq 0 ]; then exit 888; fi;'
                                
                                echo "Build has failed...Exiting the pipeline.....!!"
                                
                                sh 'exit 1' 
                            }
                                                        
                        }
                    }
                }
            }
        }
    }
}
